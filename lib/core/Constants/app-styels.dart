import 'package:flutter/widgets.dart';

class AppStyles {
  // border radius for buttons and fields
  static const  BorderRadius borderRadius= BorderRadius.all(Radius.circular(7));
  
//  primary color
static const Color primaryColor = Color(0xFF795548);
static const Color secondColor = Color(0xFFFFFFFF);

// fonts
static const String primaryFont = 'quicksand';
static const String customFont = 'CustomFont';

  
} 